knitr::opts_chunk$set(echo = FALSE)
pop_height <- rnorm(1000, mean = 1.7, sd = 0.15)
pop_height <- round(pop_height, 2)
sample_height <- sample(pop_height, 20)
sample_height
set.seed(92037)
pop_height <- rnorm(1000, mean = 1.7, sd = 0.15)
pop_height <- round(pop_height, 2)
sample_height <- sample(pop_height, 20)
sample_height
mean(sample_height)
print(mean(sample_height))
sample_mean <- mean(sample_height))
sample_mean <- mean(sample_height)
sample_mean <- mean(sample_height)
print(sample_mean)
sort(sample_height)
mode(sample_height)
median(sample_height)
print("the minimum is", min(sample_height))
print("the minimum is", min(sample_height))
print(paste("the minimum is", min(sample_height)))
print(paste("the minimum is", min(sample_height)))
print(paste("the maximum is", max(sample_height)))
quantile(sample_height)
quantile(sample_height)
median(sample_height)
sample_mean <- mean(sample_height)
print(sample_mean)
div <- sample_height - sample_mean
div2 <- div^2
s <- sum()
div <- sample_height - sample_mean
div2 <- div^2
s <- sum(div2)/20
div <- sample_height - sample_mean
div2 <- div^2
s <- sum(div2)/20
print(paste("The standard deivation of the sample is",s))
sd(sample_height)
div <- sample_height - sample_mean
div2 <- div^2
s <- (sum(div2)/20)^(0.5)
print(paste("The standard deivation of the sample is",s))
sigma <- s/(20)^(1/2)
sigma <- s/(20)^(1/2)
print(sigma)
knitr::opts_chunk$set(echo = FALSE)
dt(0.025, df = 19)
